server:
  port: 10100
spring:
  application:
    name: erueka-server

eureka:
  client:
    serviceUrl:
      defaultZone: http://192.168.31.245:${server.port}/eureka/
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false
